import React from 'react';
import { shallow, mount } from 'enzyme';
import TenDay from '../lib/TenDay';


describe('TenDay', () => {
  it('should shallow', () => {
    // const component = shallow(<TenDay />);

    // console.log( component.find('input[type="text"]').debug() );
  })
})