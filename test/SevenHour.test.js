import React from 'react';
import { shallow, mount } from 'enzyme';
import SevenHour from '../lib/SevenHour';


describe('SevenHour', () => {
  it('should shallow', () => {
    // const component = shallow(<SevenHour />);

    // console.log( component.find('input[type="text"]').debug() );
  })
})