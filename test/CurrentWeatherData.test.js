import React from 'react';
import { shallow, mount } from 'enzyme';
import CurrentWeatherData from '../lib/CurrentWeatherData';


describe('CurrentWeatherData', () => {
  it('should shallow', () => {
    // const component = shallow(<CurrentWeatherData />);

    // console.log( component.find('input[type="text"]').debug() );
  })
})